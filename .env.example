# Frontend (Vite)
# Used in src/api/supabase.ts
# Optional in current code because defaults are hardcoded, but recommended for team setup.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Server (Vercel Functions / vercel dev)
# Used by api/chat.ts, api/report.ts, api/diary.ts, api/annotation.ts, api/classify.ts
CHUTES_API_KEY=your_chutes_api_key

# Server (fallback provider for classify endpoint)
# Used by api/classify.ts when applicable
ZHIPU_API_KEY=your_zhipu_api_key
