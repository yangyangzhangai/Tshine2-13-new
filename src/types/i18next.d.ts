// i18next type augmentation
// Keys are validated at the dictionary level (en.ts / zh.ts) rather than at t() call sites,
// to avoid version-specific TS-plugin issues with the `translation:` prefix.
import 'i18next';
